#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin>>t;
    while(t--){
        string s;
        cin>>s;
        int n = s.size();
        for (int i = 1; i < n; i++)
        {
            if(s[i] != s[0]){
                swap(s[])
            }
        }
        
    }
    return 0;
}